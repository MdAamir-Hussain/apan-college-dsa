#include <iostream>
using namespace std;

int main()
{
    // Declare the integer variable
    int n;

    // Input the value of n
    cin >> n;

    // Calculate the sum using the formula
    int sum = n * (n + 1) / 2;

    // Output the result
    cout << sum << endl;

    return 0;
}
